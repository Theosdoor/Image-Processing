[project]
name = "image-processing"
version = "0.1.0"
description = "Image processing project with inpainting and classification"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "opencv-python",
]

